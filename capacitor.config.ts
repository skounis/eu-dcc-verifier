import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.betterevents.attendeescanner',
  appName: 'Attendee Scanner',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
